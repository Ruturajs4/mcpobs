# Decision log

Structural decisions (D1–D3b) are inherited from `Architecture.md` ADR-001…010,
which remains the system of record. D11+ were taken during Day-1 execution.

| # | Date | Decision | Rationale |
| --- | --- | --- | --- |
| D1 | 2026-08-14 | Collector produces to Kafka directly; no bespoke ingest service. | ADR-003. Never hand-write the most availability-critical tier. |
| D2 | 2026-08-14 | Kafka is in the path from Day 1. | ADR-002. The ack boundary and offset-commit ordering are semantics, not infrastructure. |
| D2b | 2026-08-14 | Apache Kafka, not Redpanda. | ADR-002. Apache 2.0, open-source tiered storage, Connect sinks, managed availability. |
| D3 | 2026-08-14 | Normalizer is stateless; trace assembly happens in ClickHouse. | ADR-005. No stream processor, now or later. |
| D3b | 2026-08-14 | Partition by tenant; never require trace locality or ordering. | ADR-004. Spans arrive out of order regardless. |
| D11 | 2026-08-14 | `result_type` column created but left NULL; `pending_input` dropped from taxonomy v0. | The SDK emits no `resultType` attribute, so MRTR interim results are invisible on the span. Column kept so no migration is needed when it becomes visible. |
| D12 | 2026-08-14 | Capture every scenario over **both** stdio and streamable HTTP. | Transport shapes what a customer runs; we needed to know whether attributes differ. **They do not** — the two transports emit identical attribute sets. |
| D13 | 2026-08-14 | Ship taxonomy v0 with only `ok` and `tool_error` reachable, and say so loudly. | See finding below. Inventing a distinction the telemetry cannot support would be worse than reporting the gap. |
| D14 | 2026-08-14 | `rpc_status_code` is `Nullable(String)`, not `Int32`. | The SDK sets `str(code)`. Day-1 doc §9.3 was wrong. |
| D15 | 2026-08-14 | Demo server calls downstream via plain `httpx`, not the SDK's `httpx2`. | `opentelemetry-instrumentation-httpx` patches `httpx` only; with `httpx2` the downstream child span never appears and A4 would silently pass as vacuous. |

| D17 | 2026-08-15 | Failure classification runs in the customer's process via an opt-in middleware that annotates the SDK's existing span. **No tool content is captured, transmitted or stored.** | Resolves D13 without dragging the taxonomy behind payload capture and its redaction/retention gates (V2 §15). Error intelligence stays a core feature. |
| D18 | 2026-08-15 | The classifier is a bridge, not an endpoint. Pursue an upstream SDK change that exposes the failure kind directly. | We match SDK-internal message strings; `tests/test_sdk_contract.py` fails the build if they move, but the durable fix is upstream. |
| D19 | 2026-08-15 | `protocol_error` **is** reachable — correcting D13. | `_handle_call_tool` re-raises `MCPError` rather than converting it (`mcpserver/server.py:422`). Day 1 saw no such error only because no demo tool raised one. |
| D20 | 2026-08-15 | `resultType` is read off the sealed wire form by the same middleware, **reversing D11**. | Middleware receives the serialized dict, which carries `resultType`. MRTR interim results are therefore observable, and `pending_input` is checked before any error branch so it can never enter an error rate. |
| D21 | 2026-08-15 | Record `failure_kind_source` (`helper` \| `span`) on every row. | Two different data qualities must not silently mix. A dashboard can say "12% of your servers are not running the helper" instead of quietly under-reporting. |

---

## Finding D13 — RESOLVED 2026-08-15 by D17

> **Status: resolved.** The finding below stands as written — the taxonomy is
> genuinely not reachable *from span attributes*. What changed is that it does
> not have to be: the distinguishing text is SDK-generated boilerplate present
> in the result, and a middleware can classify it in the customer's process and
> annotate the span the SDK already opened.
>
> Verified end to end: `server_exception`, `tool_error`, `unknown_tool` and
> `invalid_arguments` are now distinct on real telemetry, with zero tool content
> stored (assertions A3b, B1–B3). The V2 §25 launch gate is met.

## Original finding — the failure taxonomy is not reachable from span attributes

**What we tested.** Four deliberately distinct failure modes:

| Scenario | Intent |
| --- | --- |
| `soft_fail` | tool returns `CallToolResult(isError=True)` |
| `explode` | handler raises `RuntimeError` |
| `no_such_tool` | call to an unregistered tool |
| `echo_fast({"message": {...}})` | schema-violating argument |

**What we got.** All four produce an identical span: `status=ERROR`,
`error.type="tool_error"`, no `rpc.response.status_code`, no exception event.

**Cause.** `MCPServer`'s tool handler catches everything and converts it to
`CallToolResult(isError=True)` *before* `OpenTelemetryMiddleware` observes the
result. The middleware's `except MCPError` and `except Exception` branches
(`mcp/server/_otel.py:45-60`) are therefore unreachable for anything routed
through `tools/call`.

**Consequences.**

1. Day-1 doc §9.5 lists five categories; two are reachable.
2. V2 §25's launch checklist item *"MCP isError and thrown exception are
   distinguishable"* is **not achievable from span attributes** with the stock SDK.
3. V2 §6.3 sells an MCP failure taxonomy as a core differentiator. Today that
   taxonomy has one failure bucket.

**Options for Day 2** (none chosen yet):

- Inspect `CallToolResult` content text — distinguishes the cases, but content is
  a payload feature (opt-in, redaction-gated per V2 §15), so the taxonomy would
  inherit payload-capture constraints.
- Contribute an upstream SDK change so the middleware sees the original failure.
- Ship a documented single failure bucket for v1 and revisit.

This is the most important thing Day 1 found. It belongs on the Day-2 agenda,
not in a backlog.

| D23 | 2026-08-15 | Replay is proven, not assumed. A deliberate normalizer bug was shipped, detected, fixed, and the data corrected by reprocessing from Kafka with a fresh consumer group — no producer resent anything. | ADR-007 is the justification for the entire Kafka tier and had never been exercised. `scripts/replay.py` is the operational tool. |
| D24 | 2026-08-15 | Reads that must be correct across a normalizer change MUST resolve `argMax(..., normalization_version)` per `(trace_id, span_id)`. | Proven necessary: a naive query over replayed data mixes buggy v2 rows with corrected v3 rows and reported 6 tools where there are 5. |
| D25 | 2026-08-15 | Any `trace_locator` read must dedupe explicitly (`LIMIT 1 BY trace_id`). | ReplacingMergeTree deduplicates only when parts merge, which is asynchronous and may never have happened. A replay re-inserts every trace_id, so a naive lookup returns duplicates. Day 3's API depends on this. |
| D26 | 2026-08-15 | "Is recent data healthy?" must be asked in **event time**, never ingest time. | Replay deliberately decouples the two clocks: it re-ingests old spans *now*, so `ingested_at` cannot isolate a current run. Found when a helper-coverage assertion failed against replayed history. |

| D27 | 2026-08-15 | Record the platform's clock resolution in `docs/observed_attributes.md`, and assert duration measurability (B8) rather than trusting fast-tool latency. | 52% of local `tools/call` spans had `duration_ns = 0`. OTel timestamps spans with `time.time_ns()`, whose smallest tick on this Windows host is **0.75 ms** — so anything faster records as zero and p50 for `echo_fast` reads 0 ms. Linux is nanosecond-grade, so production is very likely unaffected; a customer on Windows would not be. Reported as WARN, not FAIL: an environment property, not a defect. |

| D28 | 2026-08-15 | **Stitch MRTR round-trips, and report BOTH numbers.** Correlate via a hash of `requestState`; never store the blob. | Measured (`scripts/mrtr_experiment.py`): one logical tool call produces **2 `tools/call` spans that do NOT share a trace_id** — the client's retry starts a new trace. The only link is `requestState`, which the server emits on the asking round and the client echoes back on the answering round. Server-observed time was **3.00 ms vs 405 ms** of real wall clock — a **135×** understatement — so reporting either number alone is wrong: "server time" is the SLO number, "logical wall clock" is the UX number. |
| D29 | 2026-08-15 | Latency eligibility is a **stored column**, not a query-time filter. | A `subscriptions/listen` duration is a stream lifetime and an MRTR interim round excludes client think-time. A `WHERE` clause gets forgotten in exactly one place and the bug ships; a column makes the default safe. |
| D30 | 2026-08-15 | `ctx.elicit()` is NOT the MRTR path. Use the `Resolve`/`Elicit` annotation. | `ctx.elicit()` issues a legacy server-initiated `elicitation/create` over a back-channel that a stateless 2026-07-28 transport does not have — it fails with `NoBackChannelError`. Worth knowing before advising a customer. |
| D31 | 2026-08-15 | A client without elicitation capability gets `-32021 MissingRequiredClientCapability`, not an interim result — confirming D19 on real telemetry. | This is a *protocol* failure, legitimately span-sourced: the `MCPError` propagates rather than being converted, so the helper never sees a result. B3 therefore excludes `protocol_error` — not a loophole, but the correct boundary of what the helper is for. |

| D32 | 2026-08-15 | The pipeline emits **metrics, not traces**, about itself (V2 §19). | Tracing the normalizer means emitting spans about the act of storing spans: volume scales 1:1 with customer traffic and our own telemetry becomes our largest tenant. Metrics are bounded by cardinality instead of by traffic — the right shape for a hot loop. |
| D33 | 2026-08-15 | Self-telemetry uses a **separate Collector pipeline** that never touches Kafka or a customer tenant. | The outage that breaks ingestion must not also blind us to it. Metrics survive a Kafka or ClickHouse failure precisely because they do not depend on either. |
| D34 | 2026-08-15 | Insert failures are counted **before** the exception is re-raised. | An insert failure that is invisible is the one that becomes silent data loss. |

| D35 | 2026-08-15 | **`opentelemetry-instrumentation-dbapi` wraps `Cursor.execute`, NOT `Connection.execute`.** The idiomatic one-liner produces no span at all. | Verified, not assumed: `connection.execute(...)` emitted zero spans; the same query through an explicit cursor emitted them correctly. This is the shape of gap a customer reports as "my database calls do not show up", so it belongs in onboarding docs before they ask. |
| D36 | 2026-08-15 | Promote `db.operation` / `db.collection`, never `db.statement`. | Statement text can contain inlined literals — customer identifiers, emails, anything a developer put in the SQL — so capturing it is payload capture under a different name (V2 §15). Note the installed instrumentation currently emits only `db.system` + `db.statement`; the operation is recoverable from the span name until semconv catches up. |
| D37 | 2026-08-15 | Derive `downstream_kind` at normalize time (`http` \| `db` \| `llm` \| `messaging` \| `internal`). | The waterfall should not re-derive it per query, and "unexplained server time" becomes a value you can filter on rather than an absence you must infer. |

| D38 | 2026-08-15 | **`insert_deduplication_token` works — but only when batch boundaries are identical.** ADR-006 covers crash recovery, not operator replay. | Proven, finally, by running ReplicatedMergeTree locally. A crashed consumer re-reads the same offsets, forms the same batch, produces the same token, and the redelivery is discarded — which is the case ADR-006 was written for. An operator replay batches differently (the live consumer flushes on a 5s timer; a replay drains everything at once), so the token differs and rows are re-inserted. **That is correct**: a bug-fix replay must produce new rows at a higher `normalization_version`, and dedup would actively prevent the fix from landing. D24's version-resolved reads are the mechanism there, not a workaround. |
| D39 | 2026-08-15 | Every insert sets `deduplicate_blocks_in_dependent_materialized_views = 1`. | Without it deduplication is **half-applied**: ClickHouse discards the duplicate block in `spans_raw` but still propagates it to dependent materialized views. A replay left `spans_raw` correct while `trace_summaries` counted 160 spans against 120 actual. Aggregates silently disagreeing with the table they are built from is the worst failure mode available — nothing errors, and every dashboard is wrong. Caught by assertion B4. |
| D40 | 2026-08-15 | Run **ReplicatedMergeTree + embedded Keeper locally**, not just in production. | Three deferrals ("test it in staging") were hiding two real defects for two days. A local environment that cannot exercise the production engine does not de-risk anything; it postpones the discovery. Cost: one config file and a longer startup. |
| D41 | 2026-08-15 | Mount the Keeper config **file**, never the `config.d` directory. | Mounting the directory shadows the image's own `docker_related_config.xml`, which is what sets `listen_host`. The server then binds to 127.0.0.1 only, so every in-network client gets "connection refused" while `docker exec` keeps working — a confusing failure that looks like a network problem. |

| D42 | 2026-08-15 | `_number()` guards every float leaving the query API; `value or 0` is banned. | **NaN is truthy in Python**, so `quantile(...) or 0` passes NaN straight through and `json.dumps` then raises — a 500. ClickHouse returns NaN from `quantile()` over an empty set, which is exactly a brand-new tenant with no telemetry. **A new customer's first-ever page load was an error.** Found only because assertion C5 checks that an unknown tenant sees nothing; no amount of local data would have surfaced it. |
| D43 | 2026-08-15 | `/errors` filters in SQL, never after pagination. | Post-filtering a page means a window whose most recent traces all succeeded returns an EMPTY error list while errors sit just past the page boundary. An error view that looks healthy is the worst lie it can tell. |
| D44 | 2026-08-15 | Every span read goes through `LATEST_SPANS`; endpoints never write SQL. | Puts D24 (version resolution) and tenant scoping in exactly one place, so neither can be forgotten by an endpoint author. Guarded by tests that assert the SQL still contains `argMax`, `{tenant:String}` and `{project:String}`. |
| D45 | 2026-08-15 | A `DELETE FROM spans_raw` does **not** propagate to materialized-view targets. | MVs fire on INSERT only. Deleting from a source table silently desynchronises every aggregate built on it — learned by doing it and permanently skewing `trace_summaries` in dev. |

| D46 | 2026-08-15 | **Freshness uses a fixed 15-minute window, ignoring the user's selected range.** | It answers "is the pipeline healthy *now*", not "what was it historically". A 24h range swept in replayed spans — whose event time precedes ingest time by hours *by design* (D26) — and reported **19,820 s** of latency. It was measuring a replay, not the pipeline. Caught by looking at the UI, not by any test. |
| D47 | 2026-08-15 | Synthetic test data lives in its own tenant (`_verify`), never in `local`. | The idempotency probe was showing up in the console as a second "server". Test fixtures must be invisible to product queries — and a separate tenant exercises the scoping while it is at it. |
| D48 | 2026-08-15 | The console is served by the query API itself: one file, no build step, no framework. | A Phase-0 proof does not need a frontend toolchain, and adding one would make it the largest thing in the repo. One process, no CORS, no separate deploy. |

| D49 | 2026-08-15 | **Error text from failing tool results is captured by default** (`capture_error_detail=False` opts out). | 140 of 146 failing spans carried no message anywhere: the SDK's reachable branch calls `set_status(StatusCode.ERROR)` with no description (D13), so `status_message` is empty and "what went wrong" was unanswerable. Bounded to 512 chars, failing spans only, and written to its own `failure_detail` column so payload capture (DF-8) stays a separate opt-in and assertion B2 still passes. **A weaker privacy default than D17's absolutism** — an exception string can contain user data — so the README discloses it in the integration snippet itself, not an appendix. Revisit when a design partner has a view. |
| D50 | 2026-08-15 | The helper recovers `mcp.resource.uri` from `params["uri"]`. | `OpenTelemetryMiddleware` derives its target from `params["name"]`, which `tools/call` and `prompts/get` have and `resources/read` does not. So resource spans said "a read happened" and never what was read — neither span name nor any attribute carried it. This is structural addressing, the analogue of a tool name, not payload. |
| D51 | 2026-08-15 | A trace opens in a **right-hand drawer over the list**, not a new page. | Debugging is comparison — you bounce between traces — and navigating away loses the list you were working through. `Esc` closes; the URL still carries `?trace=&span=` so both stay linkable. |
| D52 | 2026-08-15 | **Every alias in a generated `argMax` projection is prefixed** (`s_<column>`). | Unprefixed aliases bit three times: `AS timestamp` made the `WHERE toDate(timestamp)` resolve to the aggregate, and `AS normalization_version` made the ranking column inside every other `argMax` resolve to an aggregate. Both surface as ILLEGAL_AGGREGATION with a confusing message. Rows are read positionally, so aliases are cosmetic and prefixing eliminates the class. |
| D53 | 2026-08-15 | Tools, prompts, resources and protocol methods share **one query path** (`capabilities(kind)`) and one view. | They are the same question asked of different `mcp_method` values. Four near-identical implementations would drift apart; one cannot. `protocol` deliberately matches everything not a capability invocation, so a newly added MCP method cannot vanish the way `tools/list` did. |
| D54 | 2026-08-15 | Self-time is computed server-side and shown as a solid segment inside a faded total bar. | `total − Σ children` needs the whole trace, so it belongs where the data is. It is the difference between "this tool is slow" and "this tool waits on something slow" — `fetch_status` is 7.8ms own work inside 10.4ms total, which duration alone cannot express. |

| D55 | 2026-08-15 | **Tool request/response capture, OFF by default** (`capture_payloads=True`), truncated to 2048 chars and pattern-redacted. | Clicking a span showed no request or response, which is the first thing anyone opens a span to see. Off by default — unlike error detail (D49) — because this is every argument and every result of *every* call, not just failures: a tool named `get_customer` returning a customer record is the normal case. V2 §15 specifies opt-in, redactable, size-limited, and this follows it. The demo server enables it so the capability is visible; a real server should choose deliberately. |
| D56 | 2026-08-15 | Redaction is documented as **incomplete**, not as a guarantee. | It catches field names (`password`, `token`, `api_key`, …) and value shapes (bearer, `sk-`, `ghp_`, AWS keys, JWTs). It will not catch a secret in a field called `note`. Saying so in the docstring and README is the difference between a safeguard and a false promise. |
| D57 | 2026-08-15 | Assertion B2 changed shape: from "no payload columns are populated" to "payload capture is off by default, and the taxonomy works without it". | The original was right while the feature did not exist, but it asserted *emptiness* rather than the property that matters. Kept as B2 + B2b so the guarantee — precise classification with zero payload stored — is still checked (140 such failures). |
| D58 | 2026-08-15 | Non-text content blocks (image, audio, resource) are summarised by type, never dumped. | A base64 image is megabytes of noise that tells an operator nothing and would dominate storage. |

| D59 | 2026-08-15 | **Promote `http_url` and `db_statement`, redacted at normalize time — reversing D36.** | D36 said "never promote `db.statement`" for privacy. But it was already stored **unredacted** inside `span_attributes` and rendered in the console's raw-attribute list, so not promoting it bought nothing: the value was in the row either way, just somewhere less obvious. That is privacy theatre. Promoting it *with redaction* — and redacting it in the raw map too — is strictly safer than the status quo as well as more useful. `http.url` had the same problem and worse: `?api_key=…` query strings were flowing in untouched. |
| D60 | 2026-08-15 | HTTP request/response **bodies are not captured**, and the console says so on the span rather than showing a blank. | Two reasons: the OTel HTTP instrumentation does not record bodies, and `mcpobs` wraps the MCP *server*, not the customer's outbound HTTP client. Capturing them would mean instrumenting a different thing. The span shows what genuinely exists — method, URL, status — and names the limitation, because an empty panel reads as a bug. |
| D61 | 2026-08-15 | Request/Response in the span panel **adapts to span kind**. | Rendering only the MCP shape is why downstream calls looked empty. An HTTP span's request *is* its URL; a DB span's request *is* its statement; an LLM span's is its model and token counts. All were already stored and none were surfaced. |
| D62 | 2026-08-15 | Redaction happens **before storage, never before display**. | A secret that reaches the table cannot be recalled: it is in backups, in the Kafka replay window, and in every downstream copy. Render-time redaction protects the screen and nothing else. |

| D63 | 2026-08-15 | **Payloads are captured as the full JSON-RPC message, not a fragment.** | The first version stored `params["arguments"]` and the first text block — `{"code": 200}` / `downstream returned 200`. That is not a protocol message, and this is an MCP observability product: the useful artefact is the wire message you can compare against the spec or paste into a bug report. |
| D64 | 2026-08-15 | Capturing `_meta` recovers **client identity**, which we had written off as unavailable. | The T3 report recorded that the SDK emits no client attribute on the span, so "which client called this tool" (V2 §6.1) looked impossible. It is in `_meta.io.modelcontextprotocol/clientInfo` on **every request**, along with `clientCapabilities` — the first thing to check on a capability error — and `traceparent`. All of it was being discarded. |
| D65 | 2026-08-15 | Response capture reads the whole `result`, not `content` blocks. | Looking for `content` meant `prompts/get` (returns `messages`) and `resources/read` (returns `contents`) captured **nothing at all**. It also drops `structuredContent`, the typed result, which was invisible. |
| D66 | 2026-08-15 | `requestState` is redacted inside captured payloads. | It carries recorded elicitation outcomes — the user's actual answers (D28). We hash it for MRTR correlation *precisely because* it must not be stored; capturing the whole params object would have let it ride along in plain text. Found by asking what the new capture now includes that the old one did not. |

| D67 | 2026-08-15 | **D60 revised by measurement: HTTP *request* bodies and both sets of headers ARE captured; the response body is not.** | D60 said bodies "are not captured" because the instrumentation does not record them. True, but the wrong reason to stop: `HTTPXClientInstrumentor` takes `request_hook`/`response_hook`, a real seam nobody had looked for. Measuring it split the question. The REQUEST body is already buffered when the hook fires, so reading it consumes nothing. The RESPONSE body is unreachable *on this span* — the instrumentation wraps the transport, so the span ends when `handle_async_request` returns and httpx reads the body afterwards in `Client.send`; attributes set on an ended span are dropped in silence. D60's conclusion was right by accident for half the problem and wrong for the other half. |
| D68 | 2026-08-15 | We will **not** buffer the response body to close that gap. | It is technically possible: read the stream in the hook and hand the caller a replacement. It would also turn every streaming download in the customer's process into a full in-memory read, to satisfy our telemetry. A monitoring tool that changes the memory profile of the thing it monitors has stopped being one. The console states the gap and its reason instead. |
| D69 | 2026-08-15 | There is **no `http_response_body` column** — migration 011 drops the one 009 added. | An always-empty column is worse than a missing one: it reads as "this call had no response body", a wrong answer where we otherwise give none. Also the first real test of the migration checksum guard, which refused the tidier fix of editing 009 in place and was right to — a drifting checksum means two installations silently disagree about their schema. So 009 stays as applied and 011 carries the correction. |
| D70 | 2026-08-15 | HTTP headers are captured by **allow-list, never deny-list**. | A deny-list captures every header nobody thought of, and the one nobody thought of is where the next bespoke `x-acme-session` credential lives. `authorization` and `cookie` are not read at all, rather than read and then scrubbed. The demo deliberately sends a bearer token on `submit_order` so this is verified against the live database, not only in a unit test. |
| D71 | 2026-08-15 | **D64 unlocked: `clientInfo` is promoted to span attributes** (`mcpobs.client.name` / `.version`) and to columns. | D64 found client identity inside `_meta`; that left it reachable only through payload capture, which is off by default. Client identity would then have been a side-effect of enabling something far more invasive. Two low-cardinality strings, set on success as well as failure — recording them only on failures would answer "which clients call which tools" with a breakdown of failures, which is subtly wrong and therefore worse than silence. Self-reported and unverified per the spec: display only, never an authorisation input. |
| D72 | 2026-08-15 | **DF-12 closed: `db.operation` and `db.collection` are derived from `db.statement`.** | The instrumentation emits neither, so "which table is slow" needed someone to read every statement by eye — both were derivable all along from data we already stored. Parsed from the *redacted* form, so literals cannot leak into a derived label that a metrics backend would export beyond our redaction. Regex SQL parsing is defensible only because of what it is used for: two low-cardinality labels to group by, where anything unparseable yields empty — the status quo — and never a guess. A wrong table name would silently misattribute latency. |

| D73 | 2026-08-15 | **One rollup table, not the two DF-7 named.** `server_metrics_1m` is `tool_metrics_1m` with the tool column dropped. | Two materialized views would be two copies of the same arithmetic, free to drift, and the register's own framing invited that. With `mcp_tool_name` in the sort key, `GROUP BY service_name` answers the server question *exactly* -- `uniqExact` over a key column is not an estimate. A second table would have bought a second source of truth and nothing else. |
| D74 | 2026-08-15 | `failure_category` is part of the rollup's sort key. | Every summary view wants counts *and* a breakdown. With the category in the key the breakdown is a `GROUP BY`; without it, it needs a separate aggregate state per category. It multiplies rows by the category count, which is fixed at eight. |
| D75 | 2026-08-15 | **A materialized view cannot honour D24, so aggregates get a second maintainer: `scripts/recompute_rollups.py`.** | An MV sees an insert batch, not a version history, so a replay ADDS to its counters instead of superseding. This is not fixable inside the MV. Aggregates are therefore maintained by the MV in the normal case and rebuilt by the script after a replay -- the same class of operation as the replay itself. It uses `REPLACE PARTITION` from a staging table, so a repair never shows a hole to a reader mid-run. |
| D76 | 2026-08-15 | Assertion E1 is **split into drift and repair**. | `make verify` already replays a span (B11b). E1a asserts the drift is visible; E1b asserts the recompute fixes it. Asserting only the repair would hide that drift is possible; asserting only the drift would leave the repair untested. |
| D77 | 2026-08-15 | **`trace_summaries` had the same defect since Day 2, and B4 could not see it.** | It is a materialized view too, so it double-counted replayed spans from the start. B4 compared `sum(span_count)` against `count()` on `spans_raw` -- both populations included the duplicates, so the two errors cancelled and the assertion passed while agreeing with a double-count. Only visible once the recompute made one side correct. B4 now compares against the argMax-resolved span population, and the recompute covers both aggregates. |
| D78 | 2026-08-15 | **DF-3 closed: `trace_summaries` is partitioned by a date taken from the ROW, not from an aggregate.** | 004 recorded that "every candidate date lives in an aggregate column whose value changes as parts merge". True of `min(timestamp)`, and irrelevant: `toDate(timestamp)` of the span being inserted is deterministic and never changes. The blocker was assuming the trace's date had to be a property of the trace rather than of the row. |
| D79 | 2026-08-15 | A trace straddling midnight becomes two summary rows, and that is accepted. | The cost 004 named is real. Its frequency is not: a trace lasting T seconds straddles with probability T/86400, about one in eight million at 10ms per trace. Any reader that does `GROUP BY trace_id` merges the halves exactly -- and reading an AggregatingMergeTree already requires aggregating across unmerged parts, so a correct reader pays nothing. |
| D80 | 2026-08-15 | `trace_summaries` keeps **7-day retention matching `spans_raw`; the metric rollup gets 90 days**. | A rollup row is a statistic and stands alone. A trace summary is a LINK -- every row implies a drill-down into spans that must still exist. A summary outliving its spans would put rows in the console that 404 when clicked, which is worse than not showing them. |
| D81 | 2026-08-15 | **DF-4 closed as a product caveat: the clock tick is measured per service and the console refuses to present percentiles it cannot support.** | DF-4 sat on WATCH being reported by `make verify` every run -- which means it was told to *us* and never to the customer, while the console printed an unqualified number. The register said the decisive part itself: Linux is nanosecond-grade so our production is probably fine, **a customer on Windows is not**. That makes it a product caveat, not a test-rig footnote. |
| D82 | 2026-08-15 | The caveat has **two independent triggers**, and neither subsumes the other. | A p50 within 10x of the tick is quantisation however few zeros there are. A p50 comfortably above the tick still misleads when most calls floored to zero, because the survivors are a biased tail rather than a sample of the whole. |
| D83 | 2026-08-15 | The clock verdict is computed **server-side and shipped as a string**, not as a flag the browser interprets. | The previous banner hardcoded "~0.75ms" -- a value measured once on one laptop and frozen into the UI, wrong for every other host and silent when the clock was fine but calls still floored to zero. The tick is now measured where the spans are, and the reason travels with the number to every place it is read, including table cells. |

| D84 | 2026-08-15 | **DF-15 answered by measurement: the Collector cannot resolve an API key to trusted tenant attributes, so a thin auth gateway sits in front of it.** | `otel/opentelemetry-collector-contrib:0.115.1` ships `basicauth`, `bearertokenauth`, `oidcauth`, `asapauth`, `sigv4auth`, `oauth2client` and `headers_setter`. Every one answers "is this request allowed?"; none writes a resolved tenant onto the Resource. The authenticator interface puts data in the request context and nothing stock moves it from there. The choice was a custom Go extension or a shim; this is the shim. ADR-003's substance survives -- batching, retry, queueing, backpressure and Kafka production all remain the Collector's, which is what the ADR was actually protecting. |
| D85 | 2026-08-15 | The gateway **parses and re-serialises** every payload to overwrite `tenant.id`, rather than passing it through. | The point is that the customer's own value must not survive. Existing keys are dropped before the trusted ones are appended: appending alone would leave two `tenant.id` entries and make the winner depend on whichever consumer read last, which is a coin toss rather than a boundary. |
| D86 | 2026-08-15 | **The Collector's OTLP ports are no longer published, and its hard-coded tenant stamping is deleted.** | Both were required for the auth to mean anything. A published 4318 is an unauthenticated bypass sitting beside the front door; and the old `resource/tenant` processor would have OVERWRITTEN the authenticated tenant with `local`, collapsing every customer into one. A "fallback default" would do the same damage precisely when the gateway was bypassed, which is when you least want it -- so untenanted payloads stay untenanted and visible. |
| D87 | 2026-08-15 | Ingest and read are **separate scopes**. | They leak differently: an ingest key lives in a customer's server process and deployment config, a read key in a browser. One being compromised must not imply the other. Assertion F2 fires an ingest key at the read plane. |
| D88 | 2026-08-15 | API keys are stored as **plain SHA-256**, and that is deliberate rather than lazy. | bcrypt and argon2 exist to make brute force expensive against LOW-entropy inputs -- human passwords. This secret is 256 bits from `secrets.token_urlsafe`, so brute force is not the threat model, and a deliberately slow hash would add its cost to the authentication of every ingest request in the hot path. Hashing at all is what matters: a leaked dump must contain no usable keys. The reasoning does not transfer to a password column, and the schema says so where someone would otherwise copy it. |
| D89 | 2026-08-15 | The key-resolution cache stores the **secret hash alongside the principal**. | Caching only the principal would have let any secret through once a valid prefix was warm; re-reading Postgres to check would not have been a cache at all. Negative results are cached too, so a key-spraying attacker cannot turn authentication into a denial of service against the control plane. TTL is 30s, which IS the documented revocation delay and the Postgres-down survival window (Architecture.md §8). |
| D90 | 2026-08-15 | **Invite-only is a property of the schema, not a rule in the API.** | There is no signup table and no code path that creates a user except redeeming an invite; the redeeming UPDATE is conditional on `accepted_at IS NULL`, so two simultaneous redemptions cannot both win. An API-layer check is one forgotten branch away from an open front door. `scripts/admin.py bootstrap` is the only way to create an org and is not reachable over HTTP -- the moment an endpoint mints an org, invite-only is a policy rather than a property. Assertion F6 checks no user exists without a redeemed invite. |
| D91 | 2026-08-15 | Tenancy in the query API comes from the key and the `?tenant=` parameter is **removed, not deprecated**. | Leaving it as an override "for admins" is how a tenancy boundary becomes optional, and an optional boundary is not one. The repository was already written so endpoints never choose a tenant (Day 3), so this changed where the value comes from and nothing else -- which is the whole reason that shape was chosen then. |
| D92 | 2026-08-15 | The archive is a **Kafka consumer group**, not the Collector's `awss3` exporter. | The exporter would have been less code and would archive BEFORE Kafka -- so the archive would contain what we intended to send rather than what the pipeline actually consumed. Reading from the topic, with its own lag and its own replay position, is what makes it a faithful copy rather than a parallel opinion. |
| D93 | 2026-08-15 | The archive stores **raw OTLP protobuf, not parquet** -- deviating from Architecture.md §4. | ADR-007 settled it already: "One replay mechanism is worth paying for." Replay means feeding these bytes back through the same consumer. Parquet needs a second replay path that reads columns instead of messages, which is exactly the split ADR-007 rejected. The cost is real and stated: the archive is not directly queryable by Athena. Queryable long-term telemetry is ClickHouse's job, and a parquet projection can be derived from the archive later -- the reverse cannot. |
| D94 | 2026-08-15 | Archive objects are keyed **tenant first**, with the Kafka offset range in the filename. | Tenant first makes "delete everything belonging to this customer" a prefix delete and a per-tenant lifecycle rule a prefix rule; date first would make both a full scan, and GDPR erasure is not something to discover you cannot do cheaply. Offsets rather than a timestamp or uuid make a retry after a crash between put and commit overwrite the identical object instead of creating a second copy. |
| D95 | 2026-08-15 | Payloads are **framed with explicit lengths** before compression. | Protobuf is not self-delimiting: two concatenated `ExportTraceServiceRequest`s parse as one merged message, silently, with repeated fields joined. The archive would still look valid and would no longer contain the messages it claims to. |
| D96 | 2026-08-15 | The archiver is MCP-unaware but **tenant-aware**, and must be. | Architecture.md §4 says it does nothing MCP-aware, and it does not -- it never reads a span, a method or a payload. It does read `tenant.id` off the Resource, because one message can carry SEVERAL tenants (the Collector's batch processor merges concurrent requests) and keeping one customer's bytes out of another's object is the entire reason the key layout exists. |

| D97 | 2026-08-15 | **Downstream instrumentation is documented two ways, and the OTel agent is named first.** | `opentelemetry-instrument python -m your_server` is the better answer whenever it is available: it is OTel-native, we maintain nothing, and it picks up a new instrumentation package the day the customer installs it. Recommending our own helper first would be recommending our code over a standard that already works. |
| D98 | 2026-08-15 | **`instrument_downstream()` exists anyway, because a large share of MCP servers do not own their command line.** | They are spawned by the CLIENT over stdio -- Claude Desktop, an IDE, another agent -- so there is no command to prepend `opentelemetry-instrument` to. That case is specific enough to MCP that an in-process equivalent earns its place rather than duplicating the agent for the sake of it. |
| D99 | 2026-08-15 | It **discovers** instrumentors through the `opentelemetry_instrumentor` entry-point group rather than naming them. | That is the mechanism the agent itself uses, so `pip install opentelemetry-instrumentation-redis` is the entire integration: no mcpobs release, and no hardcoded list to forget to update. |
| D100 | 2026-08-15 | It returns a **report** and never raises. | A call that patches an unknown set of libraries and says nothing is not something anyone should put in a production server, so the customer gets `{name: outcome}` back. Each instrumentor is attempted independently: one package with a version conflict is reported and skipped, never allowed to stop a server booting. An observability library that prevents a server from starting has done more damage than the telemetry was worth. |
| D101 | 2026-08-15 | It stays **opt-in**; `instrument(server)` does not call it. | Patching a customer's database driver as a side effect of "observe my MCP server" is not something to do quietly. Same reasoning that keeps `instrument_httpx()` separate (D60). |
| D102 | 2026-08-15 | **The httpx2 claim was wrong: contrib ships `HTTPX2ClientInstrumentor` and it works.** | `demo_server/server.py` had carried a comment since Day 1 saying the SDK's httpx2 was not instrumentable, and the demo used plain httpx to work around it. Measured: the entry point exists, loads, and emits spans against a real request. So the MCP SDK's OWN transport calls are observable, which we had written off. Another instance of a limitation recorded from the absence of a feature rather than the absence of a seam (cf. D67). |
| D103 | 2026-08-15 | Order between `instrument_downstream()` and `instrument_httpx()` is **free**, and that is asserted with real requests. | OTel's `BaseInstrumentor` is a singleton whose `instrument()` no-ops while active, so body-capture hooks survive either order. Checked by making an actual HTTP request over a real socket in both directions -- inspecting `is_instrumented_by_opentelemetry` would only have confirmed that our own guard ran, which is not the question. A customer will get the order wrong half the time and nothing should depend on it. |
| D104 | 2026-08-15 | The demo bootstrap now calls `instrument_downstream()` instead of naming instrumentors by hand. | A demo that integrates differently from the documentation is how the documentation goes stale without anyone noticing. |
| D105 | 2026-08-15 | Test teardown restores the **raw** `trace._TRACER_PROVIDER`, never `get_tracer_provider()`. | With none set, that call returns the ProxyTracerProvider, and restoring the proxy into the global makes it delegate to itself -- a RecursionError 900 frames deep inside the MCP SDK, presenting as four failing classifier assertions. The bug had been latent in `test_sdk_contract.py` since Day 2 and only sprang once a second test set a provider. `set_tracer_provider` is also guarded by a `Once`, so resetting the provider without resetting the guard silently sends spans to the previous test's exporter. |

| D106 | 2026-08-15 | **`db.name` removed from the collection candidates: it is the DATABASE, not the table.** | Found by running a real query through `opentelemetry-instrumentation-psycopg` against the live Postgres, rather than by writing a synthetic span from the spec. The synthetic version passed; the real one reported `db_collection = "mcpobs_control"` -- the database -- while the table `orgs` sat parsed by DF-12's statement parser and then discarded, because an instrumentation-provided value outranks a derived one (D72). So "which table is slow" answered with the database name for EVERY driver on the shared `dbapi` integration: psycopg, pymysql, and anything else. A wrong answer rather than a missing one, which is the failure mode D72 was specifically written to avoid. `db.name` is deprecated in favour of `db.namespace` and is still available unpromoted in `span_attributes` -- it is a different dimension, not a substitute. |
| D107 | 2026-08-15 | Driver attribute shapes in tests are **copied from real instrumentation output**, not written from the spec. | The spec says what SHOULD be emitted; the test needs what IS. `tests/test_normalize.py::TestRealDriverAttributes` pins the exact attribute set psycopg produced, and it is the reason D106 was caught at all. |

| D108 | 2026-08-15 | **`result_type` was empty on every MRTR round, and the data was there all along.** | The SDK emits no `mcp.result.type`, so the helper sets `mcpobs.result.type` -- and `taxonomy.py` has always read BOTH names. `normalize.py` read only the SDK one, so the column was blank while `failure_category` sitting beside it correctly said `pending_input` and `is_latency_eligible` correctly said 0. Nothing errored, the aggregate views were right, and only the per-span column was wrong -- which is why it survived. Same shape as DF-12: present data, discarded on its way to a column. Found by asking what we actually store for MRTR rather than trusting the design doc. |
| D109 | 2026-08-15 | **`sampling/createMessage`, `elicitation/create` and `roots/list` are excluded from latency aggregates.** | These are SERVER-INITIATED: their duration is dominated by how long the CLIENT takes -- a model generating a completion, or a human approving a request. That is think-time, not a measure of how fast this server is, and it is the same reasoning that already excluded MRTR interim rounds (D28) and stream lifetimes. None has ever been observed here, because the stateless 2026-07-28 transport has no back-channel to issue them on. Handled anyway: listing them costs nothing, and not listing them costs a p95 silently poisoned by human approval time the first time a customer runs a stateful transport. |

| D110 | 2026-08-16 | **The demo opens a REAL `subscriptions/listen` stream, and assertion B9 now requires one to exist.** | B9 asserted "no stream span is latency-eligible" by counting wrongly-eligible spans and checking the count was zero. With no subscription span in the database, that was satisfied by the ABSENCE of the thing it claimed to check -- vacuously green since Day 2, against a rule only ever exercised by a synthetic unit test. Measured now: a real listen span arrives with `is_latency_eligible = 0` and category `ok`. The rule was right; the assertion was not checking it. |

| D111 | 2026-08-16 | **A cancelled call is its own category: never an error, never a latency sample.** | Measured before it was handled: a cancelled `tools/call` produced `status_code=UNSET`, `failure_category=ok`, `is_latency_eligible=1`. Two distortions, in opposite directions and both invisible -- the success count included calls that produced no result, and the percentiles included durations that measure how long the CLIENT waited before giving up. A tool cancelled *because* it is slow therefore made the p95 look better, which is the most misleading direction an error can run. Same shape as the MRTR interim round (D28) and handled the same way. |
| D112 | 2026-08-16 | The middleware catches `asyncio.CancelledError` and **re-raises immediately**. | The SDK records nothing for a cancellation: the handler's task is simply cancelled, so there is no result to classify. Catching it is the only way to see it. Swallowing it would leave a task running after asyncio has been told to stop it, breaking shutdown and cooperative cancellation everywhere downstream -- so the annotation is the only thing added, and the exception continues on its way. |
| D113 | 2026-08-16 | **`progressToken` was being redacted, because it contains "token".** | Pattern redaction was documented as incomplete (D56) in one direction only -- that it might MISS a secret. It also OVER-matches, and that failure is quieter: the field that correlates a progress notification to its request was destroyed in every captured payload, and a deleted field is indistinguishable from one the server never sent. Fixed with an explicit allow-list of protocol field names, checked first. Deliberately not a cleverer matcher: segment-aware matching would fix `author` and not `progressToken`, because "token" genuinely is a whole segment there -- the distinction is what the field MEANS, which no string rule recovers. Remaining false positives (`author`, `sessionCount`) are now stated in the source rather than unknown. |
| D114 | 2026-08-16 | `failure_kind_source` reports `helper` for ANY helper-set attribute, not just `mcpobs.failure.kind`. | A cancelled call is classified from `mcpobs.cancelled`, so it was reported as span-derived -- i.e. as evidence the helper was MISSING from a server it was demonstrably running in. That number is the product's own health metric (D21); it must not be wrong about itself. |
| D115 | 2026-08-16 | Assertion B3 derives "what counts as a failure" from the taxonomy instead of a hand-written list. | Adding `cancelled` broke B3 immediately, because it carried its own copy of the exclusions. A hand-maintained duplicate of a definition drifts the moment the definition moves -- the trap D57 named, reappearing in the assertion that guards it. |
| D116 | 2026-08-16 | Demo diagnostics go to **stderr**. | On the stdio transport stdout IS the JSON-RPC channel, and one stray line makes the client fail to parse the next message. This printed to stdout and stayed silent for weeks, because the only branch that printed was the failure branch -- which nothing triggered until `opentelemetry-instrumentation-pymysql` was installed without the `pymysql` driver beside it. A diagnostic that corrupts the protocol it is diagnosing is worse than no diagnostic. |

| D117 | 2026-08-16 | **The server-side redactor scrubbed only six predicted attribute keys; it now scrubs every attribute.** | Measured: `http.request.header.authorization = "Bearer eyJhbGciOiJIUzI1NiJ9..."` was stored VERBATIM, because `apply()` ran the credential patterns only for keys in `RISKY_KEYS`. Nothing had leaked, and the reason was not a safeguard -- nothing in this stack captures inbound headers. The protection was the absence of a feature. The authorization spec makes the exposure routine rather than exotic: `Authorization` "MUST be included in every HTTP request from client to server", so a customer setting the documented `OTEL_INSTRUMENTATION_HTTP_CAPTURE_HEADERS_SERVER_REQUEST=authorization` would have written an OAuth access token to ClickHouse on EVERY span. Per D62 that cannot be undone: it is in backups, in the Kafka replay window, and in the archive. Predicting which key a credential lands in is the bet a deny-list always loses (D70). |
| D118 | 2026-08-16 | Credential-named KEYS are replaced whole; credential-shaped VALUES are scrubbed anywhere. | Neither rule subsumes the other. A token whose shape we do not recognise -- an opaque session cookie, a bespoke scheme -- is caught only by its key; a token in an unpredicted key is caught only by its shape. Cost measured rather than assumed: 67.7 us per 13-attribute span, ~14,800 spans/sec single-threaded, against the ~2,000/sec a 10k-row/5s batch implies. Values under 15 characters skip the regex pass entirely, since the shortest match is `Bearer ` plus eight. |

| D119 | 2026-08-16 | **DF-22 closed by observing the HTTP layer beneath MCP, not by adding an attribute.** | Authorization is transport-level, so a rejected client never reaches a method and produces no MCP span. The fix is a different data source: `mcpobs.instrument_asgi(app)` wraps the ASGI app so every HTTP request becomes a server span, including the ones answered by a 401 before dispatch. |
| D120 | 2026-08-16 | It wraps an **app instance**, not a class -- because class patching silently does nothing here. | `StarletteInstrumentor().instrument()` patches `starlette.applications.Starlette`, and the MCP SDK binds that name at IMPORT time. Measured: instrument-then-import yields `_InstrumentedStarlette` with the middleware attached; import-then-instrument yields a plain `Starlette` with nothing. A customer's import order decides whether their auth telemetry exists. Worse, `instrument_downstream()` reports `starlette: instrumented` in BOTH cases, because the class patch genuinely happened -- a success report with no spans behind it. Wrapping an instance has no ordering hazard. |
| D121 | 2026-08-16 | **`unauthorized` and `forbidden` are categories but NOT failures.** | The spec's own flow diagram opens with an unauthenticated request answered by a 401: that is how a client discovers where to authenticate. Counting those as errors would make every successful OAuth handshake register as a failure -- the same trap as `pending_input` (D28) and `cancelled` (D111), now hit for the third time. `403 insufficient_scope` is the same shape: the step-up authorization flow makes it routine. Both are surfaced and not counted, so a PERSISTENT 401 rate remains a question the console can ask precisely. |
| D122 | 2026-08-16 | Only **SERVER-kind** spans are classified as authorization outcomes. | A CLIENT-kind 401 is our own outbound call to somebody else's API being rejected -- a completely different event from a client failing to authenticate to the customer's MCP server. Conflating them would put the customer's own downstream failures into their inbound auth metric. |
| D123 | 2026-08-16 | ASGI `receive`/`send` sub-spans are suppressed by default. | The instrumentation emits one of each per request alongside the server span, tripling span volume for an HTTP MCP server, and they carry nothing an MCP operator uses -- status and duration are already on the server span. `exclude_spans=[]` restores them. |

| D124 | 2026-08-16 | **DF-20 and DF-21 closed with CHILD SPANS, not span events.** | `span.add_event()` is the obvious reflex and it does not work: events ride on their span, so they are exported when the span ENDS -- precisely the moment that is already too late. A child span is exported as soon as IT ends, while the parent is still running. Measured: a 10.3-second call produced 200 progress children, and they were queryable while zero parent spans had landed. The pipeline needed no change, because trace assembly already merges a late-arriving parent with no watermark logic (Architecture.md U2). |
| D125 | 2026-08-16 | Progress emission is **capped at 200 spans per operation, and the cap announces itself**. | One span per report is fine at three reports and a denial of service at three million -- a tool reporting per row would generate spans faster than it does work. A stream that simply stops looks like the tool stopping, which is a worse lie than admitting the truncation, so hitting the cap emits one marker span. |
| D126 | 2026-08-16 | The cap counter **does not forget a span when the cap is hit.** | The first version deleted the entry there, to bound memory. That reset the count and handed the same span a fresh budget, so a flooding tool would have emitted 201 spans over and over instead of stopping -- the exact opposite of a cap. Caught by the cap test, which is the only reason it was not shipped. Memory is bounded instead by clearing the whole table past 4096 tracked parents: that can only trigger with thousands of DISTINCT parents (the memory case), never with one parent flooding (the case the cap exists for). |
| D127 | 2026-08-16 | Subscriptions are observed by **wrapping the bus**, not patching a class. | The customer already hands the bus to `MCPServer(subscriptions=...)`, so there is an explicit seam and no reason to monkey-patch -- and a customer with their own `SubscriptionBus` implementation gets it for free. Unknown methods pass through via `__getattr__`, because a wrapper that enumerates its subject's API breaks on the next SDK release. |
| D128 | 2026-08-16 | In both wrappers the customer's call runs **first**, and its result and exceptions are unchanged. | Delivery and progress reporting are the customer's functions; telemetry is ours, and ours must never delay or fail theirs. Asserted rather than asserted-about: the bus test checks the ordering explicitly. |
| D129 | 2026-08-16 | Tests that swap the global TracerProvider must restore it. | The streaming tests leaked their exporter into every test that ran afterwards, and `test_span_carries_the_failure_kind` failed while passing in isolation -- which reads as a product bug and is not one. This is the second time the global provider has caused a phantom failure (D105 was the first), so the teardown is now explicit rather than incidental. |

| D130 | 2026-08-16 | **Quotas meter SPANS, not requests.** | A request can carry one span or ten thousand, so a per-request limit lets a customer send the same volume in a hundredth of the calls and stay under it. Spans are also what the pipeline actually costs money to store, which makes them the honest unit as well as the enforceable one. |
| D131 | 2026-08-16 | Two windows -- **per minute and per day** -- because neither subsumes the other. | The per-minute limit catches the whale flooding ingest, which is the failure mode Architecture §8 names, where one tenant's burst becomes every other tenant's consumer lag. The per-day limit catches sustained overuse that no single minute would notice. |
| D132 | 2026-08-16 | The check sits **between authentication and stamping**, on the near side of the ack boundary. | That is where §5.1's write path puts it, and the position is the point: a rejection must happen before we have promised to keep anything. Checking after the ack would mean telling a customer their data was safe and then dropping it, which ADR-008 forbids in as many words. |
| D133 | 2026-08-16 | **429, not 403** -- and the difference is not cosmetic. | An OTLP exporter retries a 429 and abandons a 403, so a rate limit sent as 403 makes a client give up on data it could have delivered a minute later. `Retry-After` points at the window reset rather than a fixed guess, and the daily verdict says the truth: retrying inside the day cannot help. |
| D134 | 2026-08-16 | A **rejected burst is still counted**. | Counting only what was accepted would make a flood invisible in the counters, so the tenant being rejected would look quiet to whoever went looking for the cause. Measured: the third probe reported `used=7/5`. |
| D135 | 2026-08-16 | The soft threshold is stamped **on the span**, not just logged. | A soft quota that appears only in our logs is a warning the customer never receives. On the resource attributes it is in their own console, on exactly the data that was at risk. Fires at 80%, because a flag raised at 100% would arrive with the rejection and tell them nothing they were not about to find out. |
| D136 | 2026-08-16 | Quota counting **fails OPEN**. | If Redis is unreachable the request is allowed. Architecture §8 already settles this shape of question -- with the control plane down, "Redis-cached keys keep ingest working". Refusing a customer's telemetry because OUR bookkeeping broke inverts who is being protected, and an ingest path that fails when a cache fails is the exact fragility ADR-003 exists to avoid. An over-quota tenant getting a free minute during our outage is the cheaper mistake. |
| D137 | 2026-08-16 | Redis runs locally rather than being stubbed. | The same reasoning as embedded Keeper for ClickHouse (D40): the local rung should run the real engine. The in-process fallback is kept and documented as NOT equivalent -- it meters one gateway replica, so a fleet of four would enforce four times the limit. That is a difference to state, not to discover when the gateway first scales. |
| D138 | 2026-08-16 | Plan limits live in **code**; per-org overrides live in the **database**. | Plans are product decisions that change on a release cadence. The exception -- this customer, on this plan, with a limit support agreed to raise -- is per-tenant data. A plans TABLE would have invited someone to edit product limits with prod SQL, which is how two environments end up disagreeing about what "pro" means. The override columns are NULLable because 0 already means unlimited, so 0 cannot double as "unset". |
| D139 | 2026-08-16 | An unknown plan name falls back to the **most restrictive** plan. | A typo in a plan name must not hand somebody unlimited ingest. |

| D140 | 2026-08-16 | **`admin` is a third scope, not a flag on a read key.** | Every other scope is bounded by one org -- `read` answers "my data", `ingest` writes "my data" -- and this one is not, which makes it a different KIND of credential rather than a bigger version of the same one. Treating admin as "read, but more" would put every read key one authorization bug away from a cross-tenant view. |
| D141 | 2026-08-16 | The admin scope **cannot be granted over HTTP**; only `scripts/admin.py` mints one. | An API able to create a cross-tenant credential is one authorization bug away from a customer creating one, and there is no product reason for that endpoint to exist. Its ABSENCE is asserted rather than assumed. |
| D142 | 2026-08-16 | Cross-tenant queries live in **their own module and class** (`query/admin_repository.py`). | `query/repository.py` opens with a rule -- tenant scoping happens in that layer and nowhere else, so "an endpoint cannot forget to scope a query, because an endpoint never writes one". Adding cross-tenant methods there would have broken that sentence, and a file where MOST queries are scoped is far more dangerous than one where none are, because the reader's eye stops checking. |
| D143 | 2026-08-16 | The scope check is **one dependency on every route**, and a test asserts it is on every route. | A check repeated per endpoint is a check that will eventually be forgotten on one of them. The test was verified to FAIL against a deliberately unguarded route before being trusted -- this session has produced two assertions that passed vacuously (B9, and B3's own copy of the failure list), and a security test is the worst place for a third. |
| D144 | 2026-08-16 | The operator console is a **separate page with a separate localStorage key**, not a mode of the customer console. | A toggle inside the customer console would be one rendering bug away from showing a customer everyone else's tenants. Two pages and two credentials have no code path between them. The sidebar is amber for the same reason: an operator with both consoles open in adjacent tabs must be able to tell them apart before reading a word, because an accidental screenshot of the wrong one leaks every customer's name at once. |
| D145 | 2026-08-16 | The dashboard carries the two emergency levers Architecture §8 names -- **hard-quota a tenant, revoke a key**. | §8's mitigation for a whale flooding ingest is "hard-quota the tenant"; a lever named in the runbook and available only in a CLI is a lever someone has to go and find mid-incident. Revoke asks for confirmation because it is irreversible and takes a customer's ingest down until somebody notices. |
| D146 | 2026-08-16 | Tenants are joined **in Python** across ClickHouse and Postgres, and rows present on only one side are SURFACED. | Volume is telemetry and identity is the control plane; neither database can see the other, and pretending otherwise would mean copying one into the other. An org with no telemetry has not onboarded. Telemetry with no org should be impossible now that the gateway authenticates -- so if it appears, either a key outlived its org or something bypassed the gateway, and both are worth a page rather than a filter. |

| D147 | 2026-08-16 | **The audit entry is written in the SAME TRANSACTION as the action it records.** | This is the decision the whole design turns on, and it is available only because both rows live in Postgres. Every audit log otherwise has to answer a question badly: write the record first and the action can fail (a phantom entry); write it after and the process can die (an unlogged action); make the action depend on the log and an audit outage blocks revoking a LEAKED KEY, which is the one operation that cannot wait. Same database, one transaction, none of those trades. If Postgres is down the action was never going to succeed anyway. |
| D148 | 2026-08-16 | The old value is read INSIDE the transaction, with `FOR UPDATE`. | Reading it outside would let a concurrent change slip in between, so the entry would report a `from` that was never true -- a false statement in the one place people go for truth. |
| D149 | 2026-08-16 | The actor is stored as a **denormalised copy** (`actor_prefix`, `actor_org`) alongside the foreign key. | An audit entry that becomes anonymous when someone deletes a key is an audit entry with a delete button attached to it. The FK is `ON DELETE SET NULL` so the row survives; the copies are what keep it meaningful. |
| D150 | 2026-08-16 | **Refused attempts are recorded**, and are the most interesting rows in the table. | Somebody presenting a credential that does not work at the cross-tenant surface is exactly the event an audit log exists to surface. Best-effort, because failing to log a refusal must not turn a 401 into a 500 and tell the caller something they should not learn from an error code. The cost is stated rather than hidden: that `suppress` also swallows a coding error inside the audit call, which is why a test exercises the refusal path properly. |
| D151 | 2026-08-16 | **CLI actions are audited too.** | `scripts/admin.py` needs database access, which makes it the MORE privileged path, not one to exempt. Entries carry `actor_source = 'cli'` with a null key id, so the two paths are distinguishable without either being invisible. |
| D152 | 2026-08-16 | **Reads are NOT audited**, and the gap is stated rather than assumed. | The console refreshes every 30 seconds, so auditing list views would write thousands of rows a day whose signal is "an operator had a tab open" -- volume that would bury the mutations, which are the entries anyone will ever search for. "Who looked at customer X" is a real compliance question and this does not answer it. |
| D153 | 2026-08-16 | The audit table has **no TTL**, unlike every other table here. | An audit log that expires is one an investigation discovers empty. The rows are tiny; the retention policy that applies everywhere else does not apply to the record of who changed things. |
| D154 | 2026-08-16 | `x-forwarded-for` is recorded but explicitly **not trusted**. | A caller can set it freely, so it is an operational hint and never an authorization input. It is bounded to 64 characters so a long header cannot bloat the table. An audit field that looks authoritative and is not is worse than one that is obviously approximate. |

| D155 | 2026-08-16 | **Quota changes converge within the cache TTL, and the assertion waits for it rather than assuming it.** | Limits ride on the cached `Principal`, so a change reaches a gateway in a different process within `CACHE_TTL_SECONDS` -- the same 30s promise revocation makes. J1 probed immediately, got a 200 from the still-cached plan, and reported that quotas did not work. It now polls until the limit takes effect, which makes the loop the measurement rather than a sleep hiding a race. |
| D156 | 2026-08-16 | The dev org `local` is on the **unlimited** plan. | `trial` caps at 2,000 spans/minute and `make verify` drives several demo runs back to back, so a dev machine would start returning 429s and it would read as a pipeline fault rather than as the quota working. J1 squeezes the limit explicitly and restores it, which is how quotas stay exercised without throttling everything else. |
| D157 | 2026-08-16 | Two databases, two timestamp shapes -- and the console has to handle both. | ClickHouse serialises naive timestamps that need a `Z` to be read as UTC; Postgres `TIMESTAMPTZ` already carries an offset, and appending `Z` to that makes an invalid `Date`. Every audit row rendered as "NaNd ago". The customer console never hit it because it only reads ClickHouse -- so the bug appeared exactly where a second data source was introduced, which is where this class of bug always appears. |

| D158 | 2026-08-16 | **A subprocess pipe nobody reads is a deadlock, and both spawn sites had one.** | `stdout=PIPE, stderr=PIPE` with no reader fills the ~64KB buffer and blocks the child FOREVER -- before it binds its port. So the symptom was "the auth server never started, therefore no 401s, therefore transport authorization is broken", three inferences from the cause and none of them mentioning a pipe. Latent until `instrument_downstream()` began logging skipped instrumentors and pushed startup output past the buffer, which is why it passed in the morning and failed in the afternoon. `wait()` has the same trap as the read: it cannot reap a child whose pipe is full, so the teardown uses `communicate()`. |
| D159 | 2026-08-16 | Redis commands are a **separate set from SQL**, disambiguated by `db.system`. | `SET` is a Redis write and a Postgres session command. One merged list would read `SET search_path = public` as a Redis write and attribute it accordingly -- a wrong answer, which is the failure mode D72 exists to avoid. |
| D160 | 2026-08-16 | A Redis KEY is never promoted to `db_collection`. | `user:1042:profile` is per-user data. Putting it in a low-cardinality label would both explode the cardinality and leak an identifier into a dimension a metrics backend exports beyond our redaction (D62). Redis has no tables, so the honest value is empty. |
| D161 | 2026-08-16 | Messaging dimensions are **promoted to columns**, and the demo publishes to a real Kafka topic to prove it. | `downstream_kind = 'messaging'` had been classified correctly since U6 with nothing behind it -- the console rendered a grey tag because every field it needed was buried in `span_attributes`. Same gap DF-12 named for `db.operation`, one span kind over. The demo gained `enqueue_job` first, because a branch that ships having never rendered with data is a branch nobody has checked. |
| D162 | 2026-08-16 | API keys can expire, and `0` days means "already expired" rather than "never". | `expires_at` had been in the schema since 001 and nothing ever set it, so the authentication check that rejects expired keys had nothing to check. Testing it immediately caught a truthiness bug -- `if expires_in_days` turned `--expires-in-days 0` into no expiry at all. The same 0-vs-None trap the quota overrides handle deliberately (D138), walked straight into one file over. |

| D163 | 2026-08-16 | **The waterfall is ordered by the TREE, not by timestamp.** | Depths were computed correctly and the span list was still returned in timestamp order, so a child could be rendered above its own parent. Measured on a real trace: `tools/list` and its parent `POST /mcp` start in the same millisecond, the child sorted first, and the waterfall drew it at depth 1 above a depth-0 root -- which reads as "tools/list is a root and POST /mcp is something else". The indentation was right and the ORDER made it a lie, and indentation only means anything if the row above a child is its ancestor. Timestamp order is not a near-miss either: Architecture U2 says a child routinely lands before its parent, so this misrendered on ties AND on late parents, both normal. Siblings are still ordered by start time, which is what makes one level readable. Found by eye in the console, by the user -- no assertion covered it. |
| D164 | 2026-08-16 | A trace's header takes its tool and method from the **same span**. | Picked independently they described different operations: a trace carrying `tools/list` and `tools/call slow_export` rendered as "slow_export" with "METHOD tools/list" beside it -- a method that tool was never called by. A span with a tool wins, because that is the work the trace is about; `tools/list` is the handshake the client wraps around it. |

| D165 | 2026-08-16 | **A hidden tab refetches nothing.** | The console polled every 15s regardless of whether anyone was looking, refetching 80 trace summaries and re-running the rollup for background tabs -- which is most open tabs, most of the time. One `visibilitychange` listener removes the majority of the query load, with no architectural change and no new failure mode. |
| D166 | 2026-08-16 | Auto-refresh runs on **monitoring views only** -- Overview and Servers, never Traces or Errors. | Those two answer "is it healthy NOW". Traces and Errors are investigation surfaces: a list that reorders under the cursor while you are reading it is worse than a stale one, and the drawer already had to suppress refresh for exactly that reason. |
| D167 | 2026-08-16 | Staleness is **shown**, and stamped only after a successful fetch. | A number with no timestamp beside it is a number someone will quote as current -- and on the views that no longer poll, the indicator is the only thing saying otherwise. Stamping before the fetch would report "updated just now" over data that failed to load, and a staleness indicator that lies is worse than none because it is believed. |
| D168 | 2026-08-16 | Returning to a hidden tab refreshes **once, immediately**, rather than waiting out the interval. | Otherwise the first thing a returning user sees is up to 15s stale while the page claims to be live -- which is the failure the pause was introduced to avoid, moved to a different moment. |
| D169 | 2026-08-16 | SSE was considered and DEFERRED, with the reason recorded. | Kafka is a genuine change stream, but the raw topic is the wrong hook: a span sits there for up to 5s before the normalizer inserts it, so an event fired on arrival would tell a browser to re-query BEFORE the row exists. The correct hook is after the normalizer's insert returns (`consumer.py`, where the offset commit already marks "this data is real"), fanned out through the Redis we already run for quotas -- not a new Kafka topic, which ADR-009 would require an architecture decision for. The cost that does not go away is statefulness: Architecture §4 lists the Query API as `Stateless`, and long-lived SSE connections make it sticky. Deferred until concurrent viewers per tenant make it worth that trade. |

| D170 | 2026-08-16 | **The loading spinner appears on navigation, never on refresh.** | It blanked the content pane on EVERY render, including the 15s auto-refresh, so a dashboard nobody had touched went white and repainted four times a minute. A spinner answers "your click did something"; on a background refresh there was no click, so it answered a question nobody asked and destroyed the thing being read in order to do it. |
| D171 | 2026-08-16 | Scroll position survives a same-view refresh. | `.content` is the scroll container, so replacing its `innerHTML` snaps to the top. On a navigation that is correct; on a refresh it yanks the page away from someone reading row forty. Restored only when the view key is unchanged, so navigating still lands at the top where it should. |
| D172 | 2026-08-16 | A failed BACKGROUND refresh keeps what is on screen. | Replacing good data with an error message punishes the reader for a transient nobody triggered. The health dot turning red is the signal; the previous render stays until something better arrives. A failed FIRST load still shows the error, because there is nothing else to show. |
| D173 | 2026-08-16 | The sidebar footer labels its two metrics and states *why* refreshing is paused. | It stacked five items in 10.5px mono with no labels: `freshness 19.9s` and `classified 92%` are internal jargon that nobody outside this repo can decode, and `paused` with no reason reads as a fault rather than the deliberate behaviour of D166/D167. Labels are separate elements from values so the eye can split them without reading, and the explanation lives in a `title` because the sidebar has no room for a sentence. |
| D174 | 2026-08-16 | Customer-console auto-refresh moved from 15s to **30s**, matching the admin console. | Two intervals for the same job was an inconsistency with no reason behind it, and 15s bought nothing: the ingest floor is ~5s but the numbers on the monitoring views move on the scale of minutes, so half the refreshes repainted identical values. The footer's age stamp ticks every second regardless, so a longer interval costs no clarity about how old the data is. The interval is quoted in the tooltip by interpolation, not by hand, so the text cannot drift from the constant. |
