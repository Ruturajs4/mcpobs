.venv/
__pycache__/
*.pyc
.captures/
*.ndjson
.env

# Local development credentials. Never commit these.
.mcpobs-keys.env

# Built documentation. Generated by `make docs-build`.
/site/

# SDK build artefacts. `make sdk` regenerates them.
/dist/
*.egg-info/
.neon
.env.local
.agents/
.claude/
skills-lock.json

# Internal engineering docs -- decisions.md, deferred.md, alpha-readiness.md
# etc. name the private control-plane repo and list unpatched weaknesses.
# Kept locally, never pushed to the public repo.
/docs/
